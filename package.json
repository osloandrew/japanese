{
  "name": "japanese-dictionary",
  "private": true,
  "version": "1.0.0",
  "description": "Japanese Dictionary — vanilla JS/HTML/CSS with automated crawlable-page builds.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "build:pages": "python3 scripts/build-static-pages.py",
    "build:updates": "python3 scripts/build-updates-page.py",
    "build:frequency": "python3 scripts/build-japanese-frequency.py",
    "validate:pages": "python3 scripts/validate-static-pages.py",
    "emulators": "PATH=\"/opt/homebrew/opt/openjdk@21/bin:$PATH\" firebase emulators:start --only firestore",
    "dev": "concurrently -n app,firestore -c blue,yellow \"python3 -m http.server 8936\" \"npm run emulators\""
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "concurrently": "^10.0.5",
    "firebase-tools": "^15.28.2"
  }
}
